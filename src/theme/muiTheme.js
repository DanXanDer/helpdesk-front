import { createTheme } from "@mui/material/styles";

export const muiTheme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#151616',
    },
    secondary: {
      main: '#9c27b0',
    },
    background: {
      default: '#f9f2f2',
    },
  },
});
